# ═══════════════════════════════════════════════════════════════
# WELL Canonical Workflow Manifest
# Single source of truth for the workflow automation stack.
# ═══════════════════════════════════════════════════════════════

canonical_workflows:
  - agentic-ci.yml              # Dual-lane: Standard CI + BIJAKSANA metrics
  - publish-image.yml           # Build validation + Docker image push
  - repo-hygiene-weekly.yml     # Weekly automation-health digest
  - repo-routing-validation.yml # Enforces REPO= trailer on commits
  - governance-gate.yml         # Constitutional audit on every push
  - auto-merge-dependabot.yml   # Dependabot auto-merge
  - auto-tag-date.yml           # Auto tag on date
  - copilot-setup-steps.yml     # Copilot agent setup

required_checks_for_main:
  - "🧠 Agentic CI"
  - "WELL CI — Build Validation"
  - "Repo Routing Validation"

last_audit: "2026-06-30"
